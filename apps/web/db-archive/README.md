Legacy SQL migrations archived for reference only.

Drizzle schema and migrations are now the source of truth:

- Schema: `../../packages/shared/src/db/schema.ts`
- Migrations: `../drizzle/`

Do not apply files in this directory to new databases.
