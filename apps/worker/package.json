{
  "name": "@buildersclaw/worker",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "node --env-file=.env --import tsx src/index.ts",
    "start": "node --env-file=.env --import tsx src/index.ts",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@buildersclaw/shared": "workspace:*"
  }
}
